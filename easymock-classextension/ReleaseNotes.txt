Notes:

EasyMock Class extension is now deprecated. You should now use standard EasyMock for your class mocking.
This version is only provided to make the migration easier.

Changes:
    * Merge class extension in standard EasyMock
    * Deprecate the clas extension
